class LeaderboardEntry {
  const LeaderboardEntry({
    required this.rank,
    required this.playerName,
    required this.score,
  });

  final int rank;
  final String playerName;
  final int score;
}
